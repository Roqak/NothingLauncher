package com.nothing.core.theme

import androidx.compose.ui.graphics.Color

/** Nothing OS color tokens. */
object NothingColors {
    val Background = Color(0xFF0D0D0D)
    val Surface = Color(0xFF1A1A1A)
    val OnBackground = Color(0xFFFFFFFF)
    val OnBackgroundMuted = Color(0xFF6B6B6B)
    val Divider = Color(0xFF2E2E2E)
    val Scrim = Color.Black.copy(alpha = 0.85f)
    val InverseBackground = Color(0xFFFFFFFF)
    val InverseOnBg = Color(0xFF0D0D0D)
}
