package com.nothing.launcher.drawer

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Stub host that keeps the home screen import valid until the real drawer
 * transition is implemented.
 */
@Composable
fun AppDrawerHost(modifier: Modifier = Modifier) {
    // The real drawer is rendered conditionally inside HomeScreen.
}
